initSidebarItems({"fn":[["best_effort_topological_sort","Classic topological sorting algorithm except that it doesn't abort in case of cycles."],["get_dependency_map","Build a map of dependencies between the entries of a `Registry`. * By definition, an entry named `x` depends on `y` iff the container format of `x` in the registry syntactically contains a reference to `y` (i.e. an expression `Format::TypeName(y)`). * Dependencies can play a role in code generation in some languages (e.g. Rust or C++) where inductive definitions may require explicit \"boxing\" (i.e. adding pointer indirections) to ensure finite object sizes."],["get_dependency_map_with_external_dependencies","Same as get_dependency_map but allow to specify a set of externally-provided names to ignore."]]});